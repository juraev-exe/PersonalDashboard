services:
  personal-dashboard:
    build:
      context: ./lifeos
      dockerfile: Dockerfile
    image: personal-dashboard-local
    container_name: personal_dashboard
    ports:
      - "5173:5173"
    restart: unless-stopped
